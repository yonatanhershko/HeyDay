/**
 * Purpose: Application Theme
 *
 * This module defines the color theme for the application, including the primary color.
 */

// Application color theme object
const theme = {
  primaryColor: '#403f3e',
};

export default theme;
